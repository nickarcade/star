package com.winlator.star.ui.theme

import androidx.compose.ui.graphics.Color

val Background = Color(0xFF000000)
val Surface = Color(0xFF0D0D0D)
val SurfaceVariant = Color(0xFF1A1A1A)

// ── VEGAS Brand Palette ──
val Primary = Color(0xFFD946EF)          // Electric magenta-purple
val PrimaryVariant = Color(0xFFC026D3)   // Deeper magenta
val PrimaryDim = Color(0xFF701A75)       // Dark fuchsia
val OnPrimary = Color(0xFFFFFFFF)

// Accent palette
val Secondary = Color(0xFF22D3EE)        // Cyan
val SecondaryDim = Color(0xFF0E7490)     // Dark cyan
val Tertiary = Color(0xFFFBBF24)         // Gold — badges, highlights

// Surface tones
val OnSurface = Color(0xFFE8E8E8)
val OnSurfaceVariant = Color(0xFF999999)
val OnBackground = Color(0xFFFFFFFF)
val Error = Color(0xFFCF6679)
val Divider = Color(0xFF2A2A2A)

// Material3 tokens (previously missing)
val Outline = Color(0xFF525252)
val SurfaceTint = Primary                // Tonal elevation tint matches accent

// Glow effect — now distinct from Primary for radial/flare FX
val GlowPurple = Color(0xFFC084FC)       // Lighter lavender-purple
